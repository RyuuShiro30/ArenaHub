name: appbookinglapangan
description: A new Flutter project

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.3
  google_fonts: ^8.0.2
  webview_flutter: ^4.13.1
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.0
  cloud_firestore: ^5.6.0
  image_picker: ^1.1.2
  url_launcher: ^6.3.0
  intl: ^0.19.0
  qr_flutter: ^4.1.0
  flutter_dotenv: ^5.1.0
  shared_preferences: ^2.2.0
  url_launcher: ^6.3.0 
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - .env